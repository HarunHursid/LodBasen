@page
@model LodBasen.Pages.Gruppe.GetGruppeModel
@{
	ViewData["Title"] = "GetGruppe";
}
<h1>Grupper</h1>
@* <p>
    <a asp-page="/Gruppe/CreateGruppe">Create New Gruppe</a>
</p> *@
@* <form>
    <p>
        Search: <input type="text" asp-for="Search" />
        <input type="submit" value="Filter" />
        <a asp-page="/Gruppe/GetGruppe">Back to Full List </a>
    </p>
</form> *@
<h4 style="color:red"> Grupper</h4>
<table class="table">
    <thead>
        <tr>
            <th> Gruppe Navn </th>
        </tr>
    </thead>
    <tbody>

        @if (Model != null)
        {
            @foreach (var item in Model.Grupper)
            {
                <tr>
                    <td> 
                        <p>
                        @item.GruppeNavn
                        </p>
                    </td>
                    <td>
                        <p>
                            <a asp-page="/Barn/GruppeBarn" asp-route-id="@item.GruppeId">Børn i gruppen</a>
                        </p>
                    </td>
@*                    <td>
                        <p>
                            <a asp-page="/Barn/CreateBarn" asp-route-id="@item.GruppeNavn">Tilføj barn</a>
                        </p>
                    </td>
                    <td>
                        <p>
                            <a asp-page="/Gruppe/DeleteBarn" asp-route-id="@item.GruppeNavn">Slet barn</a>
                        </p>
                    </td>*@
                </tr>
            }
        }
</table>
